# Публикует Nginx на хост (HTTP). Подключайте к базовому compose:
#   docker compose -f docker-compose.yml -f docker-compose.http.yml up -d --build
services:
  nginx:
    ports:
      - "${HTTP_PORT:-80}:80"
